- `Ice.ConnectionInfo` now provides the `connectionId` attribute, like in the other language mappings. Previously,
  the connection ID was not available through `Connection.getInfo`.
- Fixed the marshaling of `float` sequences: a finite element value outside the float range now raises `ValueError`,
  consistent with the marshaling of a single `float` value. Previously, such elements were silently marshaled as
  infinity.
